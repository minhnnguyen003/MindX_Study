<div class="container position-absolute top-50 start-50 translate-middle">
  <div class="header">
      {{> header}}
  </div>
  <div class="content">
      <div class="home-buy"> 
        <div class="return-home position-absolute">
          <span data-bs-toggle="tooltip" data-bs-placement="top" title="Home">
              <a href="/home"><i class="fas fa-shopping-cart fs-3"></i></a>
          </span>
        </div>
        <div class="cart position-absolute">
          <span data-toggle="tooltip" data-bs-placement="top" title="Cart">
              <a href="/cart"><i class="fas fa-shopping-basket fs-3"></i></a>
          </span>
        </div>
        <div class="card mb-3">
          <div class="row g-0">
            <div class="col-md-6">
              <img src="{{fruit.image}}" class="img-fluid rounded-start" alt="image_{{fruit.name}}">
            </div>
            <div class="col-md-6">
              <div class="card-body">
                <h2 class="card-title">{{fruit.name}}</h2>
                <p class="card-text">Origin: {{fruit.origin}}</p>
                <p class="card-text">Price: {{fruit.price}}$ / 1Kg</p>
                <form action="/cart/{{fruit.id}}" method="POST">
                  <label for="amount"><p class="card-text">Amount:</p></label>
                  <input type="number" id="amount" min="1" name="amount" value="1" >
                  <h3 class="mt-3">Total: <span id="total"></span></h3>
                  <button class="buyBtn mt-2 btn btn-success" type="submit">BUY</button>
                </form>
              </div>
            </div>
          </div>
        </div>
    </div>
  </div>
  <div class="footer position-absolute">
      {{> footer}}
  </div>  
</div>

<script>
  const total = document.getElementById('total');
  const amount = document.getElementById('amount');
  total.textContent = amount.value * {{fruit.price}} + '$';
  amount.addEventListener('input', () => {
    total.textContent = amount.value * {{fruit.price}} + '$';
  })
</script>